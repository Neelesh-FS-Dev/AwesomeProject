import {View, Text, Button, ButtonProps} from 'react-native';
import React from 'react';

const ButtonComponent = (props: ButtonProps) => {
  return <Button {...props} />;
};

export default ButtonComponent;
